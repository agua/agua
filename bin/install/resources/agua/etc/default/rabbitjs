# Rabbitjs Upstart and SysVinit configuration file

# Customize location of Rabbitjs binary (especially for development testing).
#RABBITJS="/usr/local/bin/agua"

# Use RABBITJS_OPTS to modify the daemon startup options.
#RABBITJS_OPTS="--showlog 4 --printlog 5"

# If you need Rabbitjs to use a specific environment variable, it can also be specified here.
export SOME_ENVIRONMENT_VARIABLE=somevalue

